# The two public host names. They must resolve to this server's public IP.
BRIDGE_DOMAIN=bridge.example.com
HANDOVER_DOMAIN=handover.example.com

# Used by Caddy for Let's Encrypt expiry notifications.
ACME_EMAIL=admin@example.com